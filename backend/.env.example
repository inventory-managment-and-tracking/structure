PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=clothtrack
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# Vercel Postgres / Neon (auto-injected when storage is linked to the backend service)
# DATABASE_URL=
# POSTGRES_URL=

JWT_SECRET=your_super_secret_jwt_key_min_32_chars
JWT_EXPIRES_IN=8h
NODE_ENV=development
